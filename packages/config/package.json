{
  "name": "@chordially/config",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "files": [
    "index.js",
    "tsconfig.base.json"
  ],
  "scripts": {
    "lint": "node -e \"console.log('config package has no standalone lint step')\"",
    "typecheck": "node -e \"console.log('config package has no standalone typecheck step')\"",
    "test": "node -e \"console.log('config package has no standalone tests')\""
  }
}
